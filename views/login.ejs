<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <style>
        body {
            font-family: 'Roboto', sans-serif; /* Apply Roboto font to the entire body */
            background-image: url('apex-360-X8iQWD4cOaU-unsplash.jpg');
            background-size: cover;
            background-repeat: no-repeat;
            background-attachment: fixed;
        }
        .login-form {
            background-color: rgba(255, 255, 255, 0.8);
            padding: 20px;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0,0,0,0.1);
            
        }
        .container{
            padding-top: 200px;
        }
        
        .welcome-message {
            color: aliceblue;
            padding: 20px;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0,0,0,0.1);
        }
        .signup-link {
            font-size: 14px; /* Decrease the font size */
            margin-bottom: 20px; /* Add bottom margin for spacing */
            padding: auto;
            text-align: center; /* Align text in the center */
        }
        .btn-margin {
            margin-top: 5px;
            padding: 2px; /* Add top margin for spacing */
        }
        .welcome{
            color: aliceblue;
            font-family: 'Franklin Gothic Medium', 'Arial Narrow', Arial, sans-serif;
        }
        .welcome-message {
          overflow: hidden;
          font-size: 40px;
          white-space: nowrap;
          width: 0;
          animation: typing;
          animation-duration: 2.5s;
          animation-timing-function: steps(30, end);
          animation-fill-mode: forwards;
        }
          
        @keyframes typing {
          from { width: 0 }
          to { width: 100% }
        }
        @keyframes blinking {
          0% {border-right-color: transparent}
          50% {border-right-color: rgb(202, 197, 197)}
          100% {border-right-color: transparent}
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="row">
            <div class="col-md-7 d-flex justify-content-center align-items-center">
                <h4 class="welcome">Welcome to</h4>
                <p class="welcome-message">
                    
                 Mess Management System.
                </p>
            </div>
            <div class="col-md-5">
                <div class="login-form">
                    <h1 class="text-center">Login</h1>
                    <form action="login" method="post">
                        <div class="form-group">
                            <input type="username" class="form-control" name="username" placeholder="Enter username">
                        </div>
                        <div class="form-group">
                            <input type="password" class="form-control" name="password" placeholder="Password">
                        </div>
                        <button type="submit" class="btn btn-primary btn-block">Submit</button>
                    </form>
                    <div class="signup-link">
                        Don't have an account?
                        <a href="/signup" class="btn btn-secondary btn-margin">Go to signup page</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
